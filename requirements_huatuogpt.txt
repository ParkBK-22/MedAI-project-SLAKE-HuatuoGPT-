# HuatuoGPT-Vision & LLaVA-Qwen2 Optimized Dependencies
# 제작진 권장 4.40.0 대신, llava_qwen2를 정식 지원하는 4.42.2 이상을 사용합니다.
transformers>=4.42.2
timm>=0.9.12
einops
sentencepiece
shortuuid
httpx
pillow>=9.0.0
PyYAML
opencv-python
pandas
tqdm
numpy<2.0

# 가속화 및 분산 환경용
accelerate>=0.29.0
peft
protobuf

# (권장) 4090의 성능을 100% 쓰기 위해 터미널에서 별도로 설치하세요:
# pip install flash-attn --no-build-isolation